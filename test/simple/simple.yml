path: .txtai/indexes/simple  # Where to save the index
content: true
embeddings:
  path: sentence-transformers/nli-mpnet-base-v2  # Model path with prefix
  storagetype: memory  # Keep document database in memory
  storagepath: /Users/cliang/.txtai/embeddings  # Where to save document database if not in memory
  gpu: true
  normalize: true
  writable: true
  hybrid: true  # Enable hybrid search to match the reference notebook
